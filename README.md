1. Function that can create grouped data by using interval, frequency and cum frequency which calculate percentile
2. To create a function from past data points that can calculate the percentile if we add new data point on same function
3. Research about the effectiveness of the solution
